{
  "name": "<%= name %>",
  "author": "<%= author %>",
  "desc": "<%- desc %>",
  "version": "0.0.0",
  "scripts": {
    "test": "<%= tester !== 'none' ? tester : 'node test'  %>"
  <% if (gulp) { %>, "prepublish": "gulp build"<% } %>
  },
  "repository": {
    "type": "git",
    "url": "<%= repo %>"
  },
  "main": "<%= entry %>",
  "files": [ "lib" ],
  "private": <%= private %>
}
